import QuoteInformation from "./QuoteInformation";

const ReviewQuote = () => {
  return (
    <div>
      <QuoteInformation request={true} />
    </div>
  );
};

export default ReviewQuote;
